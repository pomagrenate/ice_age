name: Sync SKILL.md and rules

on:
  push:
    branches: [main]
    paths:
      - skills/iceage/SKILL.md
      - rules/iceage-activate.md
      - iceage-compress/SKILL.md
      - iceage-compress/scripts/**

concurrency:
  group: sync-skill
  cancel-in-progress: false

jobs:
  sync:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v4
        with:
          ref: main

      - name: Pull latest before making changes
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git pull --rebase origin main

      - name: Sync SKILL.md copies
        run: |
          cp skills/iceage/SKILL.md iceage/SKILL.md
          cp skills/iceage/SKILL.md plugins/iceage/skills/iceage/SKILL.md
          cp skills/iceage/SKILL.md .cursor/skills/iceage/SKILL.md
          mkdir -p .windsurf/skills/iceage
          cp skills/iceage/SKILL.md .windsurf/skills/iceage/SKILL.md

      - name: Sync compress skill
        run: |
          # Sync to skills/compress (main location for Gemini CLI)
          rm -rf skills/compress/scripts
          cp iceage-compress/SKILL.md skills/compress/SKILL.md
          sed -i 's/^name: iceage-compress$/name: compress/' skills/compress/SKILL.md
          sed -i "s|The compression scripts live in \`iceage-compress/scripts/\` (adjacent to this SKILL.md). If the path is not immediately available, search for \`iceage-compress/scripts/__main__.py\`.|This SKILL.md lives alongside \`scripts/\` in the same directory. Find that directory.|" skills/compress/SKILL.md
          sed -i 's|cd iceage-compress && python3|cd <directory_containing_this_SKILL.md> \&\& python3|' skills/compress/SKILL.md
          cp -r iceage-compress/scripts skills/compress/scripts
          rm -rf skills/compress/scripts/__pycache__

      - name: Sync compress skill to plugin
        run: |
          # Copy SKILL.md and patch name + process instructions for plugin context
          cp iceage-compress/SKILL.md plugins/iceage/skills/compress/SKILL.md
          sed -i 's/^name: iceage-compress$/name: compress/' plugins/iceage/skills/compress/SKILL.md
          sed -i "s|The compression scripts live in \`iceage-compress/scripts/\` (adjacent to this SKILL.md). If the path is not immediately available, search for \`iceage-compress/scripts/__main__.py\`.|This SKILL.md lives alongside \`scripts/\` in the same directory. Find that directory.|" plugins/iceage/skills/compress/SKILL.md
          sed -i 's|cd iceage-compress && python3|cd <directory_containing_this_SKILL.md> \&\& python3|' plugins/iceage/skills/compress/SKILL.md
          # Copy scripts verbatim
          rm -rf plugins/iceage/skills/compress/scripts
          cp -r iceage-compress/scripts plugins/iceage/skills/compress/scripts
          rm -rf plugins/iceage/skills/compress/scripts/__pycache__

      - name: Rebuild iceage.skill ZIP
        run: cd skills && zip -r ../iceage.skill iceage/

      - name: Sync auto-activation rules
        run: |
          BODY="rules/iceage-activate.md"

          # Cline + Copilot — no frontmatter, direct copy
          mkdir -p .clinerules .github
          cp "$BODY" .clinerules/iceage.md
          cp "$BODY" .github/copilot-instructions.md

          # Cursor — needs alwaysApply frontmatter
          mkdir -p .cursor/rules
          printf '%s\n' '---' 'description: "Iceage mode — terse communication, ~75% fewer tokens, full technical accuracy"' 'alwaysApply: true' '---' '' > .cursor/rules/iceage.mdc
          cat "$BODY" >> .cursor/rules/iceage.mdc

          # Windsurf — needs always_on frontmatter
          mkdir -p .windsurf/rules
          printf '%s\n' '---' 'trigger: always_on' '---' '' > .windsurf/rules/iceage.md
          cat "$BODY" >> .windsurf/rules/iceage.md

      - name: Commit and push if changed
        run: |
          git add \
            iceage/SKILL.md \
            skills/compress/ \
            plugins/iceage/skills/compress/ \
            plugins/iceage/skills/iceage/SKILL.md \
            .cursor/skills/iceage/SKILL.md \
            .windsurf/skills/iceage/SKILL.md \
            iceage.skill \
            .clinerules/iceage.md \
            .github/copilot-instructions.md \
            .cursor/rules/iceage.mdc \
            .windsurf/rules/iceage.md
          git diff --staged --quiet && exit 0
          git commit -m "chore: sync SKILL.md copies and auto-activation rules [skip ci]"
          git push
